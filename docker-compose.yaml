version: "3.8"

services:
  mautrix-claude:
    container_name: mautrix-claude
    image: mautrix-claude:latest
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - ./data:/data
    environment:
      - UID=1000
      - GID=1000
    # If you're using a postgres database, uncomment the following:
    # depends_on:
    #   - postgres

  # Uncomment and configure if you need a database
  # For SQLite (default), no database container is needed
  # postgres:
  #   container_name: mautrix-claude-postgres
  #   image: postgres:16-alpine
  #   restart: unless-stopped
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   environment:
  #     POSTGRES_USER: mautrix
  #     POSTGRES_PASSWORD: changeme
  #     POSTGRES_DB: mautrix_claude

# volumes:
#   postgres_data:
